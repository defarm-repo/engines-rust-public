# Railway service-specific configuration for cattle-robot
# This overrides the root railway.toml for the cattle-robot service

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.cattle-robot"

[deploy]
startCommand = "/app/cattle-robot"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
